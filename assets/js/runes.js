// Generated by CoffeeScript 1.6.2
(function() {
  $(function() {
    return $.extend($.fn.disableTextSelect = function() {
      return this.each(function() {
        return $(this).mousedown(function() {
          return false;
        });
      });
    });
  });

  $(document).ready(function() {
    var addGlyph, addMark, addQuintessence, addSeal;

    $(".collapse").collapse();
    $("ul.marks").on('dblclick', function(e) {
      e.preventDefault();
      console.log('toto');
      return false;
    });
    $("ul.marks li").draggable({
      appendTo: "body",
      helper: "clone"
    });
    $(".rune-page").droppable({
      drop: function(event, ui) {
        var elem, name, type;

        elem = ui.draggable[0].dataset;
        type = elem.type;
        name = elem.name;
        console.log(type);
        switch (type) {
          case "mark":
            return addMark(name);
          case "seal":
            return addSeal(name);
          case "glyph":
            return addGlyph(name);
          case "quintessence":
            return addQuintessence(name);
        }
      }
    });
    addMark = function(name) {
      var newElem;

      if ($('.rune-page div[class*=mark][class*=empty]').length) {
        newElem = $('.rune-page div[class*=mark][class*=empty]').first().removeClass('empty');
        return console.log("addMark");
      }
    };
    addSeal = function(name) {
      var newElem;

      if ($('.rune-page div[class*=seal]').find(".empty").length) {
        newElem = $('.rune-page div[class*=seal][class*=empty]').first().removeClass('empty');
        return console.log("addSeal");
      }
    };
    addGlyph = function(name) {
      var newElem;

      if ($('.rune-page div[class*=glyph]').find(".empty").length) {
        newElem = $('.rune-page div[class*=glyph][class*=empty]').first().removeClass('empty');
        return console.log("addGlyph");
      }
    };
    return addQuintessence = function(name) {
      var newElem;

      if ($('.rune-page div[class*=quintessence]').find(".empty").length) {
        newElem = $('.rune-page div[class*=quintessence][class*=empty]').first().removeClass('empty');
        return console.log("addQuintessence");
      }
    };
  });

}).call(this);
